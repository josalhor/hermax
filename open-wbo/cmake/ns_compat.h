#pragma once
// Ensure the Glucose symbols and SimpSolver declaration are visible
#include "solvers/glucose4.1/simp/SimpSolver.h"
#ifndef NSPACE
#define NSPACE Glucose
#endif
using namespace NSPACE;   // or: using namespace Glucose;  (both work since NSPACE=Glucose)

